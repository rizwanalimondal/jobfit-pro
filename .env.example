# For LOCAL development only. Get a key at https://console.anthropic.com
# NEVER commit a real key, and NEVER deploy a build that embeds one —
# anyone using the page could extract it. For public hosting, proxy the
# API through a server-side function instead (see README -> Deploying).
VITE_ANTHROPIC_API_KEY=sk-ant-your-key-here
